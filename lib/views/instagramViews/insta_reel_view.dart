import 'package:flutter/cupertino.dart';

class InstagramReelsView extends StatelessWidget {
  const InstagramReelsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}